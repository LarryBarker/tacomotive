{{#if flash}}
<div class="alert alert-dismissible alert-{{flash.type}} alert-fixed shadow">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="trie">&times;</button>
    <strong>{{flash.intro}}</strong> {{{flash.message}}}
</div>
{{/if}}